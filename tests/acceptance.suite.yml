# Codeception Test Suite Configuration

# suite for acceptance tests.
# perform tests in browser using the WebDriver or PhpBrowser.
# If you need both WebDriver and PHPBrowser tests - create a separate suite.

class_name: AcceptanceTester
modules:
    enabled:
        - WebDriver
        - AcceptanceHelper
    config:
        WebDriver:
            url: 'http://wellcommerce.dev:80/app_test.php'
            browser: firefox
env:
    test1:
        modules:
            enabled:
                - WebDriver
                - AcceptanceHelper
            config:
                WebDriver:
                    url: 'http://wellcommerce.dev/app_test.php'
                    browser: firefox
    test2:
        modules:
            enabled:
                - WebDriver
                - AcceptanceHelper
            config:
                WebDriver:
                    url: 'http://wellcommerce.dev/app_dev.php'
                    browser: firefox
    test3:
        modules:
            enabled:
                - WebDriver
                - AcceptanceHelper
            config:
                WebDriver:
                    url: 'http://wellcommerce.dev/'
                    browser: firefox
    test4:
        modules:
            enabled:
                - WebDriver
                - AcceptanceHelper
            config:
                WebDriver:
                    url: 'http://wellcommerce.dev'
                    browser: firefox
    test5:
        modules:
            enabled:
                - WebDriver
                - AcceptanceHelper
            config:
                WebDriver:
                    url: 'http://localhost'
                    browser: firefox
    test6:
            modules:
                enabled:
                    - WebDriver
                    - AcceptanceHelper
                config:
                    WebDriver:
                        url: 'http://localhost/'
                        browser: firefox
