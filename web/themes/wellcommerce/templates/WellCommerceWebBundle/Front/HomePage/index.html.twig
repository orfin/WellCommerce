{% extends 'WellCommerceWebBundle:Front:layout.html.twig' %}
{% block content %}
    {% include 'WellCommerceWebBundle:Front:breadcrumbs.html.twig' %}
    <div class="row">
        <div class="col-lg-3">
            {{ layout_box('category_menu') }}
        </div>

        <div class="col-lg-9">
            {{ layout_box('product_showcase') }}
        </div>

        <div class="col-lg-12">
            {{ layout_box('product.promotions.box') }}
        </div>

        <div class="col-lg-12">
            {{ layout_box('product.news.box') }}
        </div>

        <div class="col-lg-12">
            {{ layout_box('product.bestsellers.box') }}
        </div>
    </div>
{% endblock %}
