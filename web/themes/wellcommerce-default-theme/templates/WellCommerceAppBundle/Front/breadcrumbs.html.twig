<ul class="breadcrumbs">
    {% for crumb in breadcrumbs() %}
        {% if crumb.url != '' %}
		<li><a href="{{ crumb.url }}" class="{{ crumb.cssClass }}">{{ crumb.label }}</a></li>
        {% else %}
        <li class="{{ crumb.cssClass }}">{{ crumb.label }}</li>
        {% endif %}
    {% endfor %}
</ul>
