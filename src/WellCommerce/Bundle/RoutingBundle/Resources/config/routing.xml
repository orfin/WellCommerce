<?xml version="1.0" encoding="UTF-8" ?>

<routes xmlns="http://symfony.com/schema/routing"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://symfony.com/schema/routing http://symfony.com/schema/routing/routing-1.0.xsd">

    <route id="admin.routing.index" path="/index">
        <default key="_controller">routing.controller.admin:indexAction</default>
    </route>

    <route id="admin.routing.grid" path="/grid">
        <default key="_controller">routing.controller.admin:gridAction</default>
        <option key="expose">true</option>
    </route>

    <route id="admin.routing.generate" path="/generate">
        <default key="_controller">routing.controller.admin:generateAction</default>
        <option key="expose">true</option>
    </route>

    <route id="admin.routing.add" path="/add">
        <default key="_controller">routing.controller.admin:addAction</default>
    </route>

    <route id="admin.routing.edit" path="/edit/{id}">
        <default key="_controller">routing.controller.admin:editAction</default>
        <option key="expose">true</option>
    </route>

    <route id="admin.routing.delete" path="/delete/{id}">
        <default key="_controller">routing.controller.admin:deleteAction</default>
        <option key="expose">true</option>
    </route>
</routes>
