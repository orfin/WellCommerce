<div class="table-responsive">
    <table class="table products-table mbt60">
        <thead>
        <tr>
            <th class="text-center" colspan="2">{% trans %}cart.table.heading.product{% endtrans %}</th>
            <th class="text-center">{% trans %}cart.table.heading.price{% endtrans %}</th>
            <th class="text-center">{% trans %}cart.table.heading.quantity{% endtrans %}</th>
            <th class="text-center">{% trans %}cart.table.heading.value{% endtrans %}</th>
            <th></th>
        </tr>
        </thead>

        <tbody>
        {% for item in cartProducts.rows %}
            <tr>
                <td><img src="{{ image(item.photo, 'small') }}" alt="{{ item.name }}"/></td>
                <td><a href="{{ item.route }}">{{ item.name }}</a></td>
                <td class="text-center">
										<span class="price">
										    {{ format_price(item.price, item.currency) }}
										</span>
                </td>
                <td class="text-center">
                    <input type="text" class="form-control" value="{{ item.quantity|number_format(0) }}" disabled/>
                </td>
                <td class="text-center">
				    <span class="price">
					    <strong>{{ format_price(item.quantityPrice, item.currency) }}</strong>
					</span>
                </td>
                <td class="text-center"></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
