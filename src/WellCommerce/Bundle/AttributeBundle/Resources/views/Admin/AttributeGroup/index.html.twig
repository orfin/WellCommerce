{% extends 'WellCommerceAdminBundle:Admin:layout.html.twig' %}
{% block content %}
    <h2>{% trans %}Attribute groups{% endtrans %}</h2>

    <ul class="possibilities">
        <li><a href="{{ path('admin.attribute_group.add') }}" id="add_group" class="button" title="{% trans %}Add{% endtrans %}"><span>{% trans %}Add{% endtrans %}</span></a></li>
    </ul>

    <div class="block">
        {% if groups|length > 0 %}
            <div class="scrollable-tabs">
                <ul>
                    {% for group in groups %}
                        <li><a href="{{ path('admin.attribute_group.edit',{'id': group.id}) }}">{{ group.name }}</a>
                        </li>
                    {% endfor %}
                </ul>
            </div>
        {% endif %}
        <p>{% trans %}Click group to edit its attributes{% endtrans %}</p>
    </div>

    {% include 'WellCommerceAttributeBundle:Admin:AttributeGroup/javascript.html.twig' %}

{% endblock %}
