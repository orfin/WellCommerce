<script type="text/javascript">
    GCore.OnLoad(function() {
        $('#add_group').click(function() {
            GPrompt('{% trans %}Enter group name{% endtrans %}', function(sName) {
                var oRequest = {
                    name: sName
                };

                GF_Ajax_Request(Routing.generate('admin.attribute_group.add'), oRequest, function(oData){
                    if(oData.error != undefined){
                        return GMessage(eEvent.error);
                    }
                    window.location = Routing.generate('admin.attribute_group.edit',{id: oData.id});
                });

            });
            return false;
        });
    });
</script>