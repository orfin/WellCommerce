{% extends "layout.html.twig" %}
{% block content %}
    <h2>{% trans %}Edit language{% endtrans %} "{{ language.translation }}"</h2>
    <ul class="possibilities">
        <li><a href="{{ path('admin.language.index') }}" class="button return"><span></span></a></li>
        <li><a href="#{{ form.name }}" rel="submit" class="button"><span>{% trans %}Save and finish{% endtrans %}</span></a></li>
    </ul>
    {{ form(form) }}
{% endblock %}