{% extends "layout.html.twig" %}
{% block content %}
    <h2>{% trans %}Availability{% endtrans %}</h2>
    <ul class="possibilities">
        <li><a href="{{ path('admin.availability.add') }}" class="button"><span>{% trans %}Add{% endtrans %}</span></a></li>
    </ul>
    {{ datagrid_renderer(datagrid) }}
{% endblock %}